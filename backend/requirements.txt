# Backend (FastAPI ilova qatlami) bog'liqliklari.
# OGOHLANTIRISH: og'ir ML bog'liqliklari (torch, diffusers, transformers,
# opencv, librosa, ...) `musetalk` conda env'idan keladi — ../models/MuseTalk/requirements.txt
# ga qarang. Bu fayl faqat server qatlami uchun.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
pydantic-settings>=2.0
python-multipart>=0.0.9
numpy>=1.24
openai>=1.0
edge-tts>=6.1
# Jonli temir yo'l ma'lumoti (eticket.railway.uz) — headless brauzer sessiyasi uchun.
# O'rnatgach: python -m playwright install chromium  (+ WSL: playwright install-deps chromium)
playwright>=1.40
