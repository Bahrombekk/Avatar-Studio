# Test / CI bog'liqliklari (yengil — torch/musetalk YO'Q).
# O'rnatish: pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27          # FastAPI TestClient uchun zarur
